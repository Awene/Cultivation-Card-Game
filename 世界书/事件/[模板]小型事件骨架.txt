<%_ { _%>
<%_
// ============================================================
// 【小型事件骨架】一轮即结 · 一次性小事件合集模板
//   复制本文件 → 改文件名 → 按 ★ 注释填空 / 增删事件块即可(参照《五毒教日常》)。
//   适用: 触发即结、氛围/邂逅/日常/伏笔类小事件。
//   ——需要多轮、阶段流转、分支或收集的主线, 请改用 [模板]大型事件骨架.txt。
//   机制: 每个小事件 = 一个 if(!做过('名') && 触发条件){ 输出 event 块...插入已完成 }。
//         **不占用 事件.标题/阶段**(不阻塞梦茧等多轮大事件), 只把自己名字
//         insert 进 事件.已完成事件 做"只触发一次"的门禁。
//   规范: Doc/事件写作规范.md · 大型骨架: 世界书/事件/[模板]大型事件骨架.txt
// ============================================================

const 标题   = getMessageVar('stat_data.事件.标题', { defaults: '' }) || '';
const 已完成 = getMessageVar('stat_data.事件.已完成事件', { defaults: [] }) || [];
const 做过   = (n) => 已完成.includes(n);

// —— 触发上下文(按需增删) ——
const 身份List = getMessageVar('stat_data.身份', { defaults: [] });
const 身份文本 = Array.isArray(身份List) ? 身份List.join(' ') : String(身份List);
const 是本宗   = 身份文本.includes('五毒');   // ★ 改成你门派/身份的关键词(不需要门派限定可设为 true)
const 境界     = String(getMessageVar('stat_data.修炼进度.境界') || '凡人');
const L = 境界.includes('渡劫') ? 9 : 境界.includes('大乘') ? 8
        : 境界.includes('合体') ? 7 : (境界.includes('返虚') || 境界.includes('炼虚')) ? 6
        : 境界.includes('化神') ? 5 : 境界.includes('元婴') ? 4
        : 境界.includes('金丹') ? 3 : 境界.includes('筑基') ? 2
        : (境界.includes('炼气') || 境界.includes('练气')) ? 1 : 0; // 大境界谱系见[核心系数总表]
const 时辰     = String(getMessageVar('stat_data.时间.时辰') || '');
const 具体地点 = getMessageVar('stat_data.地点.具体地点', { defaults: '' }) || '';

// —— 关键词扫描: 扫最近6楼正文, 判断玩家近来是否"提到"某些词 ——
let scanText = 具体地点;
try {
  const 起 = Math.max(0, lastMessageId - 5);
  scanText += '\n' + getChatMessages(`${起}-${lastMessageId}`).map(m => m.message).join('\n');
} catch (e) {}
const 提到 = (kws) => kws.some(k => k && scanText.includes(k));
const 夜晚 = ['戌', '亥', '子', '丑'].some(k => 时辰.includes(k)) || 提到(['入夜', '夜里', '夜色', '深夜', '晚上']);
_%>
<%_ /* 一次性小事件仅在"无进行中大事件"时触发(标题===''), 避免打断梦茧等多轮事件 */ _%>
<%_ if (标题 === '' && 是本宗) { _%>

<%_ /* ==================== 示例① · 关键词触发(纯叙事·无数值) ==================== */ _%>
<%_ if (!做过('示例小事件A') && 提到(['关键词', '某地点', '某人名'])) { _%>
<event>
<!-- ★ 触发说明: 何时/何地/因何触发 -->
[场景标题] ${描写这段一次性邂逅/氛围; 普通文字=照抄, ${...}=让模型现编}
<!-- 纯叙事小事件无需写数值变量; 若要发一件小奖励, 照项目物品格式补面板 + insert 即可 -->
本事件为一轮即结。将下列操作原样并入<UpdateVariable>的<JSONPatch>数组(变量不可见, 逐条照做):
  { "op": "insert", "path": "/事件/已完成事件/-", "value": "示例小事件A" }
</event>
<%_ } _%>

<%_ /* ==================== 示例② · 境界门禁 + 概率触发 ==================== */ _%>
<%_ /* 关键词命中就必触发, 没命中也有小概率随机撞上——避免玩家永远碰不到 */ _%>
<%_ if (!做过('示例小事件B') && L === 1 && (提到(['某地点', '某人名']) || Math.random() < 0.3)) { _%>
<event>
<!-- ★ 触发: 炼气期(L===1)时, 提到相关词 或 30% 概率 -->
[场景标题] ${描写}
本事件为一轮即结。将下列操作原样并入<UpdateVariable>的<JSONPatch>数组(变量不可见, 逐条照做):
  { "op": "insert", "path": "/事件/已完成事件/-", "value": "示例小事件B" }
</event>
<%_ } _%>

<%_ /* ==================== 示例③ · 时间(夜晚) + 地点触发 ==================== */ _%>
<%_ if (!做过('示例小事件C') && 夜晚 && 提到(['某地点'])) { _%>
<event>
<!-- ★ 触发: 夜晚路过某地 -->
[场景标题] ${描写夜里独有的一幕}
本事件为一轮即结。将下列操作原样并入<UpdateVariable>的<JSONPatch>数组(变量不可见, 逐条照做):
  { "op": "insert", "path": "/事件/已完成事件/-", "value": "示例小事件C" }
</event>
<%_ } _%>

<%_ /* ==================== 示例④ · 前置事件门禁(需先完成某大/小事件) ==================== */ _%>
<%_ /* 用 做过('X') 串起因果: 埋伏笔、下预告、开启后续线; 后续留白交给玩家推进 */ _%>
<%_ if (!做过('示例小事件D') && 做过('梦茧') && L >= 2
        && (提到(['某地点', '某人名']) || Math.random() < 0.25)) { _%>
<event>
<!-- ★ 触发: 已完成前置事件 且 达到境界后; 只点燃/预告, 不预设走向 -->
[场景标题] ${描写这桩埋伏笔的小事; 后续留白, 让{{user}}自行推进}
本事件为一轮即结。将下列操作原样并入<UpdateVariable>的<JSONPatch>数组(变量不可见, 逐条照做):
  { "op": "insert", "path": "/事件/已完成事件/-", "value": "示例小事件D" }
</event>
<%_ } _%>

<%_ } _%>
<%_ } _%>
