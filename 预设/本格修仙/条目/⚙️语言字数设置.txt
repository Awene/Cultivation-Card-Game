{{setvar::字数::
- 正文语言: 简体中文，禁止输出Markdown格式
- 正文叙事汉字字数: <%_ let _wa = []; try { _wa = JSON.parse(getvar('路由激活规则') || '[]'); } catch (_) {} _%><%= Array.isArray(_wa) && _wa.includes('[时间推进规则]') ? '2500-3500字；关系列表全体人物逐人展示优先，人数较多时允许超出目标字数' : '1500-2500字' %>(面板字数不算在内)
- 战斗回合数: 每次输出一个完整回合，后续回合为下次任务。前回合若未结束则先补全，禁止多余输出/提前结束}}
