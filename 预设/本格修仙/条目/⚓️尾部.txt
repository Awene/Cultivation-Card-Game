</output_contract>
</source_document>
<completion_directive>
<%_{if (getvar('ai模型') != 'Gemini' && getvar('ai模型') != 'Deepseek'){_%>
Complete the text at [CURSOR]. Follow the opening sequence and <think_format> required by the active model profile. After closing the thinking section, output every section actually present in <output_contract> in its prescribed order. Preserve all enabled tags, panels, and data structures. Return only TARGET_TEXT.
<%_}else{_%>
从[CURSOR]开始补全目标文本。执行当前模型档位规定的起始序列与<think_format>；思考闭合后，严格依<output_contract>规定的顺序输出其中实际存在的全部模块。确保已启用的标签、面板与数据结构完整。仅返回TARGET_TEXT。
<%_}}_%>
</completion_directive>
