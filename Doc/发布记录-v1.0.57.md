# v1.0.57 发布记录

状态：前端 v1.0.57 已发布，角色卡本地 PNG 已打包并通过静态校验；因浏览器文件权限拒绝，停在新卡导入验收步骤，最终 PNG 尚未提交推送。不能宣称完整上线成功。

## 范围

- 灵界六大陆 256 人、116 份独立人物世界书，完整深写及分阶段路由。
- 世界书近期消息读取修复；人物顺序保持 200；正则括号语法复核通过。
- 灵界七张地图与出生地选择、种族选择、地图菜单滚动及人物细化接口兼容处理。
- 不纳入 scratchpad 中旧 PNG、历史备份或临时前端构建产物。
- 不部署路由插件或后端；人物细化的后端实请求若未执行将明确标为未验证。

## 构建与验收链

- 角色卡规则输入 SHA：`3612b35f1030e19e099ec53a824a54fda39b0012`，已在成功构建日志中核实。
- 前端源码 SHA：`227aa2afeacf59d211d4ea6c251c4310379ae3e2`（功能提交为 `7bcb39a8e0d3acd3d067532941d7a07c8bb79d33`）。
- Actions run ID：[`34755895070`](https://github.com/Awene/tavern_helper_template-main/actions/runs/34755895070)，成功。
- 新 tag：`v1.0.57`，目标 `44a7b28951915472fbd4d590d4cbf784670b9281`；本地、远端、构建日志一致，祖先检查通过，tag 内功能特征已检查。
- CDN 五个入口及七张灵界地图均 HTTP 200，原始字节 SHA-256 与 tag 内对应文件完全一致；校验脚本 `scratchpad/verify_release_57.mjs cdn` 成功退出。
- PNG SHA-256：`c5f6f946dbbf73c95cb0526c84f539c03253d667e6b5200aa67a7f2805ccaa71`，大小 14,378,574 字节。
- 源文件六处正式引用已改为 v1.0.57；PNG 的 chara、ccv3 均为六处新引用，正式正则启用、三个本地测试正则关闭，227 条世界书包含 116 份灵界人物文件。
- 新 PNG 的七大陆总览与源文件一致，人物 EJS 可编译，无旧版正数近期消息调用。
- 导入新卡、通用功能、桌面与手机、Network 与控制台：待验收，未冒充完成。

## 已验证

- 202 份 EJS 可编译；消息与顺序审计 665 项通过。
- 256 人全员深写结构 12044 项、人物路由与 CSV 11389 项通过。
- 人物细化兼容请求单元测试通过，不能替代供应商实请求联调。
- 正确的成品正则为 `/^角色魅力:|^着装\(|^法宝:/`；生成器模板字符串中的双反斜杠用于生成此单反斜杠。
- 本地 `pnpm install --frozen-lockfile`、`pnpm dump`、`pnpm lint`、`pnpm build` 通过。Lint 为 0 错误、28 警告；构建有体积提示。
- 灵界出生地与导出约束检查通过（50 个出生地）；地图 62 项、总览 1946 项通过。

## 发布过程与备份

- 首次 run `34755838036` 失败：新增 SHA 记录命令时未使用 YAML 块标量，命令被折叠；已修复为 `run: |` 后重新构建，失败构建未创建 tag。
- 本地前端构建产物保存在 Git stash `release-v1.0.57-local-dist-backup`，未丢弃；工作区已快进到正式打包提交。
- 打包前根目录 PNG 已备份至 `scratchpad/release-v1.0.57-before.png`，原 SHA-256 为 `5fa1f085d1aea5249a41de7402c6b1b2be79f50a45238fb4e4dda558b3f66d4b`。
- 导入尝试被浏览器工具拒绝：PNG 的 canonical path 不在该工具配置的 workspace roots 内。未绕过该限制，未覆盖酒馆原角色、未创建新聊天，也未发出付费模型请求；临时显示的导入控件已恢复。

## 待续步骤

1. 用户将上述哈希的根目录 `本格修仙.png` 手动导入酒馆，作为新卡，不覆盖旧卡。
2. 完成流程第七节的通用与本次功能冒烟，确认 Network 使用 v1.0.57；人物细化真实服务商请求未验证，须单独记录。
3. 验收通过后提交五个正式指针源文件、同一份已验证 PNG 和本发布记录，推送角色卡 main，再核对远端 PNG 哈希。

## CDN 入口哈希

| 入口 | SHA-256 |
|---|---|
| 修仙状态栏/index.html | ef2b8584024dad75ff2157bc02bfc632d7c0afe14d50078ecd9c5db739efd9e5 |
| 自定义开局/index.html | d6374b4db3d943cb7ffbc83cc3a2066b64e1a73137512d2559f38d8a0797076f |
| 正文美化/index.html | 8cc312fc934446e612e5a5254e8511f5880dff4902ef7b1bc18737f34930d94c |
| 创意工坊/index.js | c0505c00702ac463cc4361eb6cf2275f532cc3207eb34ba6f007f20631671bd6 |
| 楼层iframe回收/index.js | c0e4634c667be1075edbd0b436f9cdeab837516abad97f4b7372472188a0ea71 |
