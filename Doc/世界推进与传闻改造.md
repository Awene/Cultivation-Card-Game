# 世界推进与传闻改造（2026-09-06，待统一发布）

## 数据与迁移

传闻为对象，内含 `上次世界推进时间点`（年、月、日、时辰，可为 null）与 `条目` 字典。按 2026-09-07 最终版设计，以传闻标题为键，每条仅含 `类别`、`内容`、`难度`；不再保存 id、时间区间、世界、地域、地点字段。

变量结构预处理器、前端 Zod Schema 和 MVU 核验均兼容旧顶层数组及旧条目数组：从内容生成标题，重名加序号防覆盖，旧地点并入内容，旧“通缉魔修”归为“通缉逃犯”、“灵植奇遇”归为“素材奇遇”。新字典重复核验保持稳定。MVU 核验仅在推进时间点为空且当前日期有效时填入当前时间；旧存档不追溯补算整个历史。普通回复不能自动覆盖已记录的起点。

前端删除 `timeline-engine.ts` 及自动同步入口，不再生成、剪枝或用旧 localStorage 缓存覆盖传闻；已有浏览器缓存不做破坏性清理，但不再读取。

## 操作与路由

传闻页新增“世界推进”、上次推进时间及当前时间。按钮从最新楼层读取变量，经角色卡脚本校验后填入输入框并自动发送。已有草稿、正在生成、日期无效、时间相等或倒退时拒绝发送；一个时辰的有效跨度也允许。

确定性指令：`请明确以[时间推进规则]进行世界演进。`

仅检测本次玩家输入；重生成、swipe、继续时从对应玩家指令及此前变量快照恢复区间。缺少此前快照时拒绝重生成，避免复用已结算的最新状态。

路由插件 0.19.5 仅提供通用接口：`window.CultivationRuleRouter.requestFixedRoute(enabledNames, reason, options)`，返回一次性请求 id，可通过既有 `cancelRequest(id)` 撤销。固定结果只开启指定候选，关闭其余候选，不展开关联，不修改常驻条目的持久开关。`registerFixedRouteResolver(id, callback)` 允许调用方在生成前提供固定选择，注销函数用于切卡清理；`options.data` 仅作为不透明 JSON 存入楼层 `extra.crr_route.requestData`，插件不解释其业务含义。

新增卡内脚本 `脚本/【本格修仙】世界推进.js`，已登记在 `本格修仙.yaml`。触发短语、时间比较、草稿/重复结算保护、重生成快照、变量核验提示全部在此实现。前端调用 `window.parent.CultivationWorldAdvance.send()`；卡内脚本指定唯一候选 `[时间推进规则]`，请求 `reuseForExtraApi: true`。卸载脚本时清理注册、事件及桥接对象，不影响其它角色卡。插件不再含 `world-advance.js` 或专用 `sendWorldAdvance()`。

移除配置中的修炼/突破关联时间推进；普通模型路由误选时间推进或旧配置关联它时也不会开启。额外变量 API 复用该正文固定结果，优先于普通 skip/off 策略；显式内部请求仍遵守自身控制。

推进时间点由 AI 与人物、传闻更新在同一 JSONPatch 中写为请求记录的终点，脚本不自动标记完成。遗漏时提示；有未结算的世界推进记录时阻止按钮重复发送，建议等待变量更新或检查并重生成。重生成应由 MVU 原有楼层回退机制恢复旧数据，插件不自行叠加更新。

## 规则、提示词与展示

- 只补算上次推进至当前的区间，不额外推进玩家时间，不重复累计已经结算的年龄、修为和物品。
- 推演前关系列表中全部“人物”逐一输出小 `time_info` 面板，接各自经历叙事；必要时补世界变化，最后回到当前玩家。陨落者先展示后按原规则移除。
- 不足一年按实际尺度叙述；人物镜头不等于主角知情，秘密不能直接成为公开传闻。
- 传闻按标题维护，保留有效旧消息并补新；失效由 AI 根据剧情判断，不再按日期字段筛选。类别表及 10—15 条的维护要求集中在变量更新规则，独立传闻更新条目已移除；运行时不强制截断或自动生成条目。
- 正文字数目标为 2500—3500，面板不计；全员展示优先，人数多时允许超出。其他回合保持原字数。
- 变量更新规则按路由显示传闻结构；未开启时只读。额外记录模型只在世界推进时收到传闻数据。
- 世界推进时剧情模型也能读取各人物的物品等细节，用于准确推演；普通回合继续尊重前端“细节可见”设置。生日等本地核验字段仍隐藏。
- 正文小面板复用并压缩原渲染，兼容旧大面板；传闻页展示类别、标题、内容、难度，手机布局分层，长标题自动换行。保留上次推进和当前时间、世界推进按钮。

## 验证与上线边界

### 2026-09-07 容错补充

- 标题原文不改名，JSON Pointer 的转义只解码一次，保留点、引号、方括号、反斜杠及字面 `~1`；传闻标题不参与根字段纠错。原始 JSONPatch 中未转义的标题斜杠在传闻结构明确的情况下合并恢复。危险原型路径拒绝执行并记录警告。
- 难度范围如 `筑基初期-筑基后期` 保留显示，不擅自取高低值；数组范围连接显示，缺失难度补为“待查”，其他缺失文本字段也有默认值。
- 时间支持中文年月日、正月/冬月/腊月、十二时辰简称、钟点及带上午/下午等前缀的钟点。例：17点→酉时，0点→子时，21点→亥时，凌晨2点→丑时。模糊词固定近似：早→卯时，白天/日间→午时，晚/晚间→戌时。
- 更新命令中完全不可识别的时间保留旧值并告警；旧存档没有有效旧值时，结构校验仍使用既有默认时间兜底。解析不额外推进日期，月日仍遵循原有 12 月、每月 30 日截断规则。
- 脚本及前端 schema 同步；测试 `node scratchpad/test_rumor_tolerance.mjs` 验证两端一致、特殊标题增改删、时间样例、幂等性和错误时间保留。

已完成：时间比较和拒绝条件、旧数组迁移与幂等核验、预处理器迁移、提示词条件分支模拟、固定路由与实际条目开关、额外 API 复用、旧关联误触发防护、重生成区间、草稿保护与按钮防连点测试。原 MVU 核验及容错回归通过。真实 Vue 组件编译与前端定向 ESLint 检查通过。

隔离编译脚本：`node scratchpad/build-world-advance.mts`，只构建状态栏与正文美化到测试目录，禁用自动 schema dump、角色卡打包和同步插件。测试：`node scratchpad/test_world_advance.mjs`、`node scratchpad/test_mvu_verifier.mjs`、`node scratchpad/test_mvu_robustness.mjs`。

Chrome 已检查从真实组件和渲染函数生成的示例预览，390px 手机页面无横向溢出，逐人面板及叙事顺序正常。预览由 `scratchpad/render-world-advance-preview.mjs` 生成；它是只读示例，不调用 AI。

尚未进行真实 AI 回合联调；本次没有改正式指针、打包角色卡 PNG、提交、推送或正式发布。后续统一发布时需同步三部分：角色卡世界书/脚本/预设（须携带新增世界推进脚本）、前端、通用路由插件及其配置。使用新 PNG 和相应预设、配置完成真实世界推进及额外变量 API 联调后再上线。

2026-09-06 本地测试同步：路由插件 0.19.5 的 5 个运行/说明文件已复制到本机 `SillyTavern/data/default-user/extensions/cultivation-rule-router`，逐文件 SHA256 一致；保留 `.git`、用户配置及 API Key。原文件备份在 `C:/Users/11700/AppData/Local/Temp/crr-before-local-0195-20260906-161522`。本地状态栏已定向重编译，仍从 5500 加载，不改正式引用。

已刷新酒馆，并通过酒馆助手的角色卡脚本接口为当前“本格修仙”添加上述新脚本（保留原 8 个脚本）。Chrome 验证 `fixedRouteVersion === 1`、通用注册接口及 `CultivationWorldAdvance.send` 可用，旧的插件专用方法不存在；指定路由请求创建后立即撤销，未发送 AI 请求。角色卡源 YAML/JS 同步更新，后续可照常从工作区打包。
