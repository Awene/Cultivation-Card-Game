{{setvar::时间地点::
<tp>
${...}
</tp>}}<tp_format>
用<tp> </tp>标签包裹的时间地点栏
插入位置
1.在每次正文开头，**必须**输出一个时间地点栏
2.文中时间地点大幅度变化时插入时空栏
格式：
<tp>
$修仙历${yyyy}年-${mm}月-${dd}日-${时辰} @ ${世界}-${地域}-${具体地点}| ${天气及特殊气候/现象}
</tp>
天气变动规则:
1. 自然气候或超自然现象
2. 随时间推移、地理跨越发生符合逻辑的动态变化
3. 短词组合
</tp_format>