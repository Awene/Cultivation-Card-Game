{{setvar::变量f::
- 变量处理: <UpdateVariable>由外部模型负责；本回复不得生成、解析或预写变量代码。}}{{setvar::变量g::禁止生成变量声明
声明: 若有UpdateVariable代码块，始终由外部模型处理；本回复不可输出、解析或预写变量代码}}{{setvar::变量c::
Variable Update Prohibition
Disclaimer:
`UpdateVariable` is handled by an external model. Do not generate, parse, or prewrite variable updates in this completion.
}}
