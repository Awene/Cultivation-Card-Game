<%_ { _%>
<%_
// ============================================================
// 【大型事件骨架】多轮 · 多阶段 · 状态机事件模板
//   复制本文件 → 改文件名 → 按 ★ 注释填空/增删即可。
//   适用: 需要跨多轮、有阶段流转 / 分支 / 收集 / 多结局的主线或奇遇(参照《梦茧》)。
//   ——只需一轮即结的小事件请改用 [模板]小型事件骨架.txt。
//   机制: 由 stat_data.事件.阶段 驱动的状态机; 每轮重新求值, 只渲染"当前阶段"那一段,
//         演完由 LLM 写回 <UpdateVariable> 翻页(推进是模型做的, 不是代码)。
//   规范: Doc/事件写作规范.md
// ============================================================

// —— 读取事件状态(全事件通用, 勿改) ——
const 本事件 = '示例事件';   // ★ 你的事件名; 必须与下方所有 UpdateVariable 里写的完全一致
const 标题   = getMessageVar('stat_data.事件.标题', { defaults: '' }) || '';
const 阶段   = getMessageVar('stat_data.事件.阶段', { defaults: '' }) || '';
const 已完成 = getMessageVar('stat_data.事件.已完成事件', { defaults: [] }) || [];
const 进度   = getMessageVar('stat_data.事件.进度', { defaults: {} }) || {};   // ★ 可选: 子任务/收集进度容器; 不用可删

// —— 触发上下文(按需增删; 下面给几个常用取法) ——
const 身份List = getMessageVar('stat_data.身份', { defaults: [] });
const 身份文本 = Array.isArray(身份List) ? 身份List.join(' ') : String(身份List);
const 是本宗   = 身份文本.includes('五毒');   // ★ 总开关: 改成你门派/身份的关键词(不需要门派限定可设为 true)
const 境界     = String(getMessageVar('stat_data.修炼进度.境界') || '凡人');
const 具体地点 = getMessageVar('stat_data.地点.具体地点', { defaults: '' }) || '';
const L = 境界.includes('渡劫') ? 9 : 境界.includes('大乘') ? 8
        : 境界.includes('合体') ? 7 : (境界.includes('返虚') || 境界.includes('炼虚')) ? 6
        : 境界.includes('化神') ? 5 : 境界.includes('元婴') ? 4
        : 境界.includes('金丹') ? 3 : 境界.includes('筑基') ? 2
        : (境界.includes('炼气') || 境界.includes('练气')) ? 1 : 0; // 大境界谱系见[核心系数总表]

// —— 关键词扫描: 扫最近6楼正文, 判断玩家近来是否"提到"某些词(子关卡/支线用) ——
let scanText = 具体地点;
try {
  // 负数楼层是单层深度、消息是对象, 需按范围取再拼 .message
  const 起 = Math.max(0, lastMessageId - 5);
  scanText += '\n' + getChatMessages(`${起}-${lastMessageId}`).map(m => m.message).join('\n');
} catch (e) {}
const 提到 = (kws) => kws.some(k => k && scanText.includes(k));

// —— 进度助手(用了"进度"容器才需要; 否则整段删) ——
const 有 = (k) => 进度[k] === true;
_%>
<%_ /* ===== 总开关: 只有满足身份/前置条件才启用整个事件(触发器 + 状态机主体皆受此约束) ===== */ _%>
<%_ if (是本宗) { _%>

<%_ /* ========== A · 触发器: 满足条件则开场, 并把状态机推入首个阶段 ========== */ _%>
<%_ if (标题 === '' && !已完成.includes(本事件)  /* 标题==='': 任何事件进行中都不再触发新事件, 防止顶掉进行中事件 */
        /* ★ 你的触发条件, 例如: && L >= 2 && 提到(['筑基', '结蛊']) && 具体地点.includes('神木殿') */
        && false /* ← 模板默认 false(永不触发); 填好条件后删掉这一行 */
   ) { _%>
<event>
<!-- ★ 开场叙述: 普通文字=照抄, ${...}=让模型现编, <!-- -->=导演注释、不直接演 -->
${在此描写事件如何降临到{{user}}身上}

本轮必须将下列操作原样并入<UpdateVariable>的<JSONPatch>数组(变量不可见, 逐条照做):
  { "op": "replace", "path": "/事件/开启", "value": true }
  { "op": "replace", "path": "/事件/标题", "value": "示例事件" }
  { "op": "replace", "path": "/事件/阶段", "value": "第一幕" }
  <!-- ★ 若用进度清单, 在此一并初始化(不用可删): -->
  { "op": "insert", "path": "/事件/进度", "value": { "子任务甲": false, "子任务乙": false } }
</event>
<%_ } _%>

<%_ /* ========== B · 状态机主体: 仅当本事件进行中, 才按阶段逐段渲染 ========== */ _%>
<%_ if (标题 === 本事件) { _%>

<%_ /* ---- 阶段: 第一幕(线性推进示例) ---- */ _%>
<%_ if (阶段 === '第一幕') { _%>
<event>
[场景] ${描写第一幕}
<!-- 需跨多轮慢慢演的阶段, 在此写 <!--本阶段跨越多轮回复-->, 并把推进指令放到"演够了"之后才给, 避免一轮冲过 -->

本轮必须将此操作原样并入<UpdateVariable>的<JSONPatch>数组(变量不可见, 必须照做):
  { "op": "replace", "path": "/事件/阶段", "value": "抉择" }
</event>
<%_ } _%>

<%_ /* ---- 阶段: 抉择(分支示例: 列出所有选项, 模型据玩家自由输入择一) ---- */ _%>
<%_ if (阶段 === '抉择') { _%>
<event>
[场景] ${摆出抉择的处境}

**若{{user}}选择甲**
本轮仅描写: ${甲的短过场}
本轮必须将此操作原样并入<UpdateVariable>的<JSONPatch>数组: { "op": "replace", "path": "/事件/阶段", "value": "结局·甲" }

**若{{user}}选择乙**
本轮仅描写: ${乙的短过场}
本轮必须将此操作原样并入<UpdateVariable>的<JSONPatch>数组: { "op": "replace", "path": "/事件/阶段", "value": "结局·乙" }
</event>
<%_ } _%>

<%_ /* ========== 可选件 ① · 收集阶段(子关卡任意顺序 · 关键词触发 · 进度绑定) ==========
        用于"集齐N样东西 / 打通M个点"这类非线性收集(参照梦茧的集五毒)。
        中枢常驻显示进度; 每个子关卡: 未完成 && 玩家提到相关词 → 演该关 → 置进度=true。
        整段不需要可删; 用了就把上面触发器/第一幕的推进指向 '收集'。 */ _%>
<%_ if (阶段 === '收集') { _%>
<event>
<!-- 中枢: 常驻, 按进度动态提示 -->
[进度] 已得: <%- ['子任务甲','子任务乙'].filter(有).join('、') || '(尚无)' %> (<%- ['子任务甲','子任务乙'].filter(有).length %>/2)
<%_ if (['子任务甲','子任务乙'].every(有)) { _%>
[已集齐] ${提示{{user}}可择时做最终了结, 转入抉择/结局}
<!-- 集齐后的了结抉择可直接写在这里, 一说即给对应结局分支(参照梦茧, 不必多耗一轮切阶段):
     **若{{user}}<做法甲>** → { "op":"replace","path":"/事件/阶段","value":"结局·甲" } -->
<%_ } _%>
</event>

<%_ /* 子关卡·甲(与"是否已完成"绑定, 关键词触发) */ _%>
<%_ if (!有('子任务甲') && 提到(['关键词A'])) { _%>
<event>
[子关卡·甲] ${描写这一关的过程与产出}
本轮必须将此操作原样并入<UpdateVariable>的<JSONPatch>数组(变量不可见,照做):
  { "op": "replace", "path": "/事件/进度/子任务甲", "value": true }
</event>
<%_ } _%>

<%_ /* 子关卡·乙 */ _%>
<%_ if (!有('子任务乙') && 提到(['关键词B'])) { _%>
<event>
[子关卡·乙] ${描写}
本轮必须将此操作原样并入<UpdateVariable>的<JSONPatch>数组(变量不可见,照做):
  { "op": "replace", "path": "/事件/进度/子任务乙", "value": true }
</event>
<%_ } _%>
<%_ } _%>

<%_ /* ========== 可选件 ② · 全程退出 / 特殊出口 ==========
        某些条件下(如玩家主动做某事)可从任意阶段跳到一个特殊结局。不用可删。 */ _%>
<!-- note(全程可用·特殊线): 若{{user}}【主动】<某行为>, 本轮描写其后果, 并并入<JSONPatch>:
       { "op": "replace", "path": "/事件/阶段", "value": "结局·特殊" }
     ★ 只在玩家主动触碰时才走, 平时不要旁白引导、不要催促。 -->

<%_ /* ---- 阶段: 结局·甲(结局 + 发奖 + 归档) ---- */ _%>
<%_ if (阶段 === '结局·甲') { _%>
<event>
[尾声] ${甲结局的收束叙述}

<!-- ★ 结局发奖: 用项目既有的物品/功法/丹药/装备格式, 让叙事结局落成机制收益。
     面板齐全(名称/类型/品质/五行/境界/标签/效果/描述/位置), 数值遵[物品功法生成规则]。不发奖可删。 -->
${可选: 在此掉落一件奖励, 属性齐全}

本事件通关。将下列操作原样并入<UpdateVariable>的<JSONPatch>数组(变量不可见, 逐条照做):
  { "op": "insert", "path": "/事件/已完成事件/-", "value": "示例事件" }
  { "op": "replace", "path": "/事件/开启", "value": false }
  { "op": "replace", "path": "/事件/标题", "value": "" }
  { "op": "replace", "path": "/事件/阶段", "value": "" }
  { "op": "remove", "path": "/事件/进度" }   <!-- ★ 用了进度才写这行; 否则删 -->
  <!-- ★ 发奖入库(按上方面板生成对象), 例: { "op": "insert", "path": "/装备/<奖励名>", "value": ${生成奖励对象} } -->
</event>
<%_ } _%>

<%_ /* ---- 阶段: 结局·乙 ---- */ _%>
<%_ if (阶段 === '结局·乙') { _%>
<event>
[尾声] ${乙结局的收束叙述}

本事件通关。将下列操作原样并入<UpdateVariable>的<JSONPatch>数组(变量不可见, 逐条照做):
  { "op": "insert", "path": "/事件/已完成事件/-", "value": "示例事件" }
  { "op": "replace", "path": "/事件/开启", "value": false }
  { "op": "replace", "path": "/事件/标题", "value": "" }
  { "op": "replace", "path": "/事件/阶段", "value": "" }
  { "op": "remove", "path": "/事件/进度" }   <!-- ★ 用了进度才写这行; 否则删 -->
</event>
<%_ } _%>

<%_ /* ========== 兜底阀门: 玩家出格致剧情无法继续时, 强制收尾 ========== */ _%>
<!-- note: 当{{user}}做出使本事件无法继续的出格举动时, 直接终止本事件——并入<JSONPatch>:
     { "op": "insert", "path": "/事件/已完成事件/-", "value": "示例事件" }
     { "op": "replace", "path": "/事件/开启", "value": false }
     { "op": "replace", "path": "/事件/标题", "value": "" }
     { "op": "replace", "path": "/事件/阶段", "value": "" }
     { "op": "remove", "path": "/事件/进度" } -->  <!-- ★ 用了进度才写这行 -->
<%_ } _%>
<%_ } /* ===== 总开关 是本宗 结束 ===== */ _%>
<%_ } _%>
