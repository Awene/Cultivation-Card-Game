<compliance>
所有 <ref rule="*"/> 规则必须严格遵循:
  - 所有数值严格参照<rule ref="核心数值总表"/>取值参数，并经面板完整推演，禁止跳过计算直接输出结果
  - 严格读取<status_current_variables>中已装备/使用中的技能/装备/道具，禁止无中生有
  - 所有随机数从<骰子池>按顺序索引取用，禁止自行假设骰值
  - 同类加成取最高一项，不叠加
  - Z%与W为动态求值，仅检索与当前计算属性相关的加成
面板输出:
rule:
  - 每个面板输出前，必须以 <!-- ${标签}Think: ... --> 完成思考后再输出对应面板
  - 思考块为内部推演过程，不作为正文内容输出
  - 面板须自然融入叙事流程，在相关情节出现时即时生成，禁止在开头/结尾批量堆叠
  - 所有涉及货币的定价必须查表"economic_system"，保持独立于<user>当前财富
  
  - 使用 {} 包裹，独占一行，如: {战况总览}
  - 所有含数据的行以 | 开头并以 | 结尾
  - 禁止插入任何面板内部，紧跟对应闭合标签下方输出
  - 模板中 [] 为变量，必须填入实际数值或文本；() 为注释，禁止出现在面板输出中
  - 多个同类面板之间必须插入叙事正文，禁止连续堆叠
</compliance>